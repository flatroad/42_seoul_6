#include "push_swap.h"

void push_swap(stack *A, stack *B, int count, int argc)
{
	if (count == 1)
	{
		while (argc / count * 2)
		{
			if(A->top->date > B->top->date)
			{
				
			}
		}
	}
	push_swap(stack *A, stack *B, int count/2);
}